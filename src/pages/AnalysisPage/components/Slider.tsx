import { Image, Card, Text, Group, Button, rem } from '@mantine/core';
import { Stack } from "@mantine/core";
import { SliderInfo } from "../../../utils/SliderInfo";

const Slider = () => {

  return (
    <Stack spacing={16}>

    </Stack>
  )
}

export default Slider;